deixar depois do SO na memoria de instrucoes
e ai chama esse codigo
void dispatcher_save(int position){
    int i;
    i = 63;
    while(i >= 0){
        position = position-(63-i);
        salvar_reg(i,position);
    }
    save pc
}

void dispatcher_load(int position){
    int i;
    i = 63;
    while(i >= 0){
        position = position-(63-i);
        carregar_reg(i,position);
    }
    carrega pc
}