/* Calcula o n-ésimo número da sequência de Fibonacci de forma recursiva */
int fib(int n) {
    if (n <= 1) {
        return n;
    }
    else{
    }
    return fib(n - 1) + fib(n - 2);
}

void main(void) {
    int num;
    num = input(); 
    output(fib(num)); 
}