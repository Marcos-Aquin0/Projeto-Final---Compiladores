int fatorial(int n) {
    int resultado;
    resultado = 1;
    while (n > 1) {
        resultado = resultado * n;
        n = n - 1;
    }
    return resultado;
}

void main(void) {
    int num;
    num = input(); 
    output(fatorial(num));
}